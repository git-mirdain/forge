In `crates/forge-github/src/export.rs` L227, `review.target.base.as_deref().unwrap_or(\"main\")` silently targets `main` when a review has no base ref. Repositories using `master`, `develop`, or other trunk branches will get PRs opened against the wrong branch with no warning. The fallback should be configurable via `GitHubSyncConfig` or surfaced as an `unexportable` warning.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)