In `crates/git-forge/src/sync.rs` L26–50, all four `RemoteSync` methods use RPITIT (`-> impl Future`), which makes the trait non-object-safe. `dyn RemoteSync` cannot be used, so selecting between GitHub, GitLab, or other adapters at runtime requires a wrapper enum. Consider boxing the futures or using `async_trait` if dynamic dispatch is ever needed.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)