In `crates/git-forge/src/exe.rs` L1813, `parse_display_id` falls back to `u64::MAX` when the numeric part can't be parsed. A malformed display ID (e.g. `GH#abc`) silently sorts to the end of every list and corrupts the zero-pad width calculation. The function should return `Option<u64>` or `Result` and callers should handle the failure explicitly.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)