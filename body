**No graceful shutdown or signal handling**: The binary calls `service.waiting().await` which blocks until the transport closes. There is no signal handler for SIGTERM/SIGINT, no cleanup logic, and no timeout. If the parent process (editor) dies without closing stdin, the server will hang indefinitely. The `forge-server` crate has PID-file guard logic; this binary has nothing comparable.