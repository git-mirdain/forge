In `crates/git-forge/src/sync.rs` L12–23, `SyncReport` tracks `imported`, `exported`, `skipped`, `failed`, and `unexportable` but has no `updated` field. An entity that already exists locally but was modified on the remote would be incremented as `skipped`, making it impossible to tell from the report whether a sync run actually applied any changes.\n\nGenerated-by: Claude Code (Claude Sonnet 4.6)